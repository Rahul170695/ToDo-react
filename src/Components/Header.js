//import Card from '../UI/Card';
import classes from './Header.module.css';

const Header = () => {
    return (
      <div className={classes.heading} >
        <h1 className={classes.content} >Let's Plan the day!</h1>
      </div>
    );
  }
  
  export default Header;
  